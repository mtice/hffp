Use cases for Semigroup/Monoid: 

- parallelization, partitioning, chunking,

- incremental, appending.

- css classes. (Append can be defined as anything). in css, class string append was just add space between classes.
- Distributed logging. Look up timestamp and append somewhere. Or just append and sort after
- Diagrams (Elm graphics system) append box and circle in an order. New diagram. append all elements results as new whatever.
- Network service. Buffered window. then(). 
- Merging DB Transactions.

Tools for Modeling Domain.

React virtual Dom. Chain of combining virtual doms fragments appended.
